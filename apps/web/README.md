# Aarogya Research Console

Minimal Next.js research UI. Not a product. Not a landing page.

```bash
pnpm --filter @aarogya/web dev
```

Requires inference-api at `NEXT_PUBLIC_API_BASE_URL` (default `http://localhost:8000`).
