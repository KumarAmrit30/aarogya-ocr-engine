# Configs

Declarative, versionable experiment knobs. No hardcoded absolute paths — use repo-relative paths.

| Path | Purpose |
|------|---------|
| `engines/` | Per-engine inference configs |
| `training/` | Training hyperparameters |
| `evaluation/` | Eval configs |
| `benchmarks/` | Comparative suite configs |
