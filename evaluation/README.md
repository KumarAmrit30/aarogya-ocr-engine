# Evaluation

Single-run metrics, runners, and reports.

Install: `pip install -e "./evaluation[dev]"`

Package: `aarogya_evaluation` — see `docs/Evaluation.md`.

Feeds into `benchmarks/` for comparative suites.
