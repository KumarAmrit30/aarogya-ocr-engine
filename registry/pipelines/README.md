# Pipeline registry

Pipelines are versioned compositions of adapters (opaque engine/component refs).

Benchmark **pipelines**, not bare engines.

| ID | File |
|----|------|
| `PIPELINE-#####` | `PIPELINE-#####.yaml` |

See `docs/adr/ADR-0002-pipelines-are-registry-driven.md`.
