# Shared TypeScript contracts

Zod schemas and types mirroring `aarogya_core.types` / API DTOs (`schema_version: "1.0"`).

Python remains source of truth for domain objects; keep this package in sync until OpenAPI codegen lands.
